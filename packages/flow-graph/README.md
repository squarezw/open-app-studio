# @oas/flow-graph

Interaction Flow Graph (IFG) types, JSON Schema bindings, and graph operations: `merge` (trace → graph), structural `fingerprint`, `diff`, `subgraph`, `replayScript` (flow → Maestro YAML).

- Data model: [docs/interaction-flow-graph.md](../../docs/interaction-flow-graph.md)
- Schema: [schemas/ifg.schema.json](../../schemas/ifg.schema.json)

Status: not yet scaffolded — see [Roadmap M0](../../docs/roadmap.md).
